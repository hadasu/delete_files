<Window x:Class="FolderApp.ProgressBar"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FolderApp"
        mc:Ignorable="d"
        Title="ProgressBar" Height="100" Width="300">
    <!--<Window.DataContext>
        <local:ProgressBarViewModel/>
    </Window.DataContext>-->
    <Grid>
        <ProgressBar Minimum="{Binding Path=Minimum}" Maximum="{Binding Path=Maximum}" Value="{Binding CurrentProgress}" Height="50" Width="300" />
    </Grid>
</Window>
